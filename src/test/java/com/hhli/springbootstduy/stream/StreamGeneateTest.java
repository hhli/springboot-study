package com.hhli.springbootstduy.stream;

/**
 * @author 李辉辉
 * @date 2019-12-13 15:42
 * @description 流生成测试
 */
public class StreamGeneateTest {

    public static void main(String[] args) {
        //System.out.println(Arrays.toString(IntStream.range(0,10).toArray()));
        //System.out.println(Stream.iterate(0, n->n+1).limit(10).collect(Collectors.toList()));

        //List<Integer> tempList = IntStream.range(0, 10).boxed().collect(Collectors.toList());
        //
        //long count = tempList.stream().map(e->{
        //    System.out.println("raw" + e);
        //    return e;
        //}).count();
        //
        //System.out.println(count);


    }
}
